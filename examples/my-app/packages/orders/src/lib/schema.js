import { CreateLib } from 'react-microfrontend';

export default {
  interface: {
    examples: {
      initialState: null,
    },
    addExample: {
      type: CreateLib.TYPE.FUNCTION,
    },
  },
};
